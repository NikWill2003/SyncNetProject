defaults:
  - coalitions_base
  - _self_

# No communication (lower bound)
name: coalitions
forward_args: {}

gate: no_comm

tok_dim: 32
cmd_dim: 16
hidden_dim: 64
msg_dim: 32
head_hidden: 0
message_proj: shared   # gate is the sole routing bottleneck (per_pair = ablation)

# baseline-gate knobs
gate_hidden: 64
ctrl_dim: 64
key_dim: 32
