# @package _global_
# Same-capacity control: STATIC learned addresses, no phase dynamics.
defaults:
  - /experiment/sqoop/busnet/_base
  - _self_
model:
  theta_init: learned
  coupling: none
  learn_omega: false
  omega_init: 0.0
  drive: none
