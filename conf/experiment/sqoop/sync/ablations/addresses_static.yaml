# @package _global_
# Ablation: one axis moved off the canonical cell (addresses:static).
defaults:
  - /experiment/sqoop/sync/_family
  - override /model: sqoop/busnet
  - _self_
model:
  addresses: static
