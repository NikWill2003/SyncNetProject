# @package _global_
# Ablation: one axis moved off the canonical cell (medium:lines,gate:attn).
defaults:
  - /experiment/sqoop/sync/_family
  - override /model: sqoop/busnet
  - _self_
model:
  medium: lines
  gate: attn
