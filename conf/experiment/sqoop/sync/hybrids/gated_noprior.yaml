# @package _global_
# Gated without the question-only prior term in the readout.
defaults:
  - /experiment/sqoop/sync/hybrids/_family
  - override /model: sqoop/gated
  - _self_
model:
  readout_prior: false
