# @package _global_
# PIXELS, all-synchrony perception, SILENT bus: what the field's own computation gives with no communication at all.
defaults:
  - /experiment/sort_of_clevr/sync_d/_base
  - _self_
model:
  encoder: {name: field}
  per_module_gru: false
  bus_phase: zero
