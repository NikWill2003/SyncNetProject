# @package _global_
# PIXELS, attention-from-state read, full-access private lines (the real
# bandwidth ceiling).
defaults:
  - /experiment/sort_of_clevr/sync_d/_base
  - _self_
model:
  encoder: {name: cnn, ch: 128, hidden: 64}
  medium: channels
  gate_mode: full
