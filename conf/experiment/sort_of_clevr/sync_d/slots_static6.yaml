# @package _global_
# PIXELS, slot attention, d=6 static addresses.
defaults:
  - /experiment/sort_of_clevr/sync_d/_base
  - _self_
model:
  encoder: {name: slots, ch: 128, hidden: 64}
  per_module_gru: false
  phase_repr: vector
  osc_dim: 6
  theta_init: learned
  coupling: none
  learn_omega: false
  omega_init: 0.0
