# @package _global_
# FOLLOW-UP 1/3 -- 1 run. d=8 with the stimulus: does the gain grow with
# slack beyond one axis per object, or was d=6 the point.
defaults:
  - /experiment/sort_of_clevr/sync_c/_base
  - _self_
model:
  phase_repr: vector
  osc_dim: 8
  drive: stimulus
