# @package _global_
# BUSNET all-synchrony ceiling -- 1 run. Same field and phase slots with
# per-sender attention on private lines.
defaults:
  - /experiment/sort_of_clevr/sync_c/_base
  - _self_
model:
  encoder: {name: field}
  per_module_gru: false
  phase_repr: vector
  osc_dim: 4
  medium: channels
  gate_mode: attn
