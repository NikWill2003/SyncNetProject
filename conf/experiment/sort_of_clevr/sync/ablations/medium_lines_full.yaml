# @package _global_
# Ablation: one axis moved off the canonical cell (medium:lines,gate:full).
defaults:
  - /experiment/sort_of_clevr/sync/_family
  - override /model: sort_of_clevr/busnet
  - _self_
model:
  medium: lines
  gate: full
