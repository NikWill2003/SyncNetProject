# @package _global_
# The SoC winner without the question-only prior head: the head is initialised from the
# question, so the term should be redundant -- this checks accuracy is unchanged.
defaults:
  - /experiment/sort_of_clevr/sync/hybrids/_family
  - override /model: sort_of_clevr/identity_busnet
  - _self_
model:
  claim_prior: spatial
  readout_prior: false
