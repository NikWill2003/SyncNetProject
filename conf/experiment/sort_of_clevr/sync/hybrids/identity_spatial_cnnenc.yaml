# @package _global_
# The Sort-of-CLEVR canonical model (learned spatial bias) on the shared cnn
# stem instead of the 4-layer field trunk: the stem row of the ablation table.
defaults:
  - /experiment/sort_of_clevr/sync/hybrids/_family
  - override /model: sort_of_clevr/identity_busnet
  - _self_
model:
  claim_prior: spatial
  encoder:
    name: cnn
    ch: 24
