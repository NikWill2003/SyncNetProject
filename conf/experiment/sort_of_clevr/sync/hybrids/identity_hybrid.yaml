# @package _global_
# Both fixes at once -- the candidate headline model: residual private
# cells (individuation without gradient starvation) + spatial claim prior
# (assembly aid) on IdentityBusNet, per-module anchors on as always.
defaults:
  - /experiment/sort_of_clevr/sync/hybrids/_family
  - override /model: sort_of_clevr/identity_busnet
  - _self_
model:
  private_cells: residual
  claim_prior: spatial
