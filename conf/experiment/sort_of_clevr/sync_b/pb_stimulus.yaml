# @package _global_
# PHASEBIND 6b/9 -- 1 run. Token phases initialised from features and
# driven by an AKOrN stimulus (phase becomes a function of content
# rather than only of the dynamics).
defaults:
  - /experiment/sort_of_clevr/sync_b/_base
  - override /model: sort_of_clevr/phasebind
  - _self_

model:
  tok_phase_init: encoder
  tok_stimulus: true
