# @package _global_
# PHASEBIND on OBJECTS 7 -- 2 runs. More modules than objects: do surplus
# modules idle, or split objects? READ: test_binding/obj_coverage,
# module_purity, eval_model/module_use.
defaults:
  - /experiment/sort_of_clevr/sync_b/_base
  - override /model: sort_of_clevr/phasebind
  - _self_
model:
  encoder: {name: objects, ch: 128, obj_size: 5}
  tok_coupling: global
  n_modules: 6

hydra:
  sweeper:
    params:
      model.n_modules: 8,12
