# @package _global_
# PHASEBIND on OBJECTS 8 -- 1 run. Pure phase read (no content similarity
# at all): a module reads whatever is in phase with it.
defaults:
  - /experiment/sort_of_clevr/sync_b/_base
  - override /model: sort_of_clevr/phasebind
  - _self_
model:
  encoder: {name: objects, ch: 128, obj_size: 5}
  tok_coupling: global
  n_modules: 6
  phase_read: only
