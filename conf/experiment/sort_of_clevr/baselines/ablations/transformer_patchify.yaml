# @package _global_
# Paper-form transformer: a single patchify conv instead of the shared stem.
# Reported in the appendix as the stem ablation for the token models.
defaults:
  - /experiment/sort_of_clevr/baselines/thesis/transformer
  - _self_
model:
  encoder:
    name: patchify
    ch: 128
    patch_size: 5
