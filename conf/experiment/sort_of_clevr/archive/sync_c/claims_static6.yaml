# @package _global_
# CLAIMS 2/4 -- 2 runs (seeds 1, 2; seed 0 = .851 / .616). Same capacity,
# fixed learned axes, no dynamics: the control the +.15 ternary is
# measured against.
defaults:
  - /experiment/sort_of_clevr/sync_c/_base
  - _self_
model:
  phase_repr: vector
  osc_dim: 6
  theta_init: learned
  coupling: none
  learn_omega: false
  omega_init: 0.0

hydra:
  sweeper:
    params:
      train.seed: 1,2
