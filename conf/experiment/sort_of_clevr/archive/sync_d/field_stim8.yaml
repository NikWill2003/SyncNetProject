# @package _global_
# PIXELS, all-synchrony perception, d=8 + stimulus (d=8 beat d=6 on objects).
defaults:
  - /experiment/sort_of_clevr/sync_d/_base
  - _self_
model:
  encoder: {name: field}
  per_module_gru: false
  phase_repr: vector
  osc_dim: 8
  drive: stimulus
