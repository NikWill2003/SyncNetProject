# @package _global_
# PIXELS, attention-from-state read, d=6 static learned addresses: the
# same-capacity control the phase gain is measured against.
defaults:
  - /experiment/sort_of_clevr/sync_d/_base
  - _self_
model:
  encoder: {name: cnn, ch: 128, hidden: 64}
  phase_repr: vector
  osc_dim: 6
  theta_init: learned
  coupling: none
  learn_omega: false
  omega_init: 0.0
