defaults:
  - sqoop_conv_lstm_base
  - _self_

# Bahdanau et al.'s no-routing floor
name: sqoop_conv_lstm
emb_dim: 32
q_pool: lstm       # mlp | lstm
fusion: spatial    # spatial | readout

lstm_hidden: 128
hidden_dim: 256

encoder:
  name: cnn
  ch: 128
  hidden: 64
