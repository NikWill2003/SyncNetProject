defaults:
  - sqoop_base
  - _self_

# Tiny dataset for smoke tests. Separate dir so it never clobbers real data.
name: sqoop
seed: 1
root: ./data
dir: sqoop-smoke
rhs_variety: 2
num_repeats: 4
num_repeats_eval: 1
eval_split: val_seen
test_split: test_unseen
